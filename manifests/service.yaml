apiVersion: v1
kind: Service
metadata:
  name: playground-service
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: playground-app
  ports:
    - protocol: TCP
      port: 8888 # This is the port exposed within the cluster
      targetPort: 8888 # This is the port exposed by the container
